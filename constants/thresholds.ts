/* eslint-disable @typescript-eslint/no-unused-vars */

/**
 * The maximum allowed image size in megabytes.
 *
 * @constant {number} MAXIMAGESIZEMB
 * @default 0.8
 */
const MAXIMAGESIZEMB: number = 0.8 * 1024 * 1024; // Maximum image size in MB
